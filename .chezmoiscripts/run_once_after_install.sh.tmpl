#!/bin/bash

{{ if eq .chezmoi.osRelease.id "fedora" -}}
sudo ~/.scripts/fedora_gnome_install.sh
{{- end }}

# Install brew
sudo -v && NONINTERACTIVE=1 /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

source ~/.bash_profile
